if(window.showForm){
	$("#project_form").hide();
	window.showForm = false;
	$('#add_project').html("Add Project");
} else {
	$('#project_form').html("<%= j (render 'form') %>");
	$("#project_form").show();
	window.showForm = true;
	$('#add_project').html("Close");
}